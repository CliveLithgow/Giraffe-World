// Harborline — site JS
// Currently just a hook point. TinySurveys WebSDK Content Navigation testing
// relies on the JSON-LD Article block + og:type=article emitted per-page by
// _layouts/article.html — no JS is required for that detection to fire.

document.addEventListener("DOMContentLoaded", function () {
  // no-op placeholder — safe to extend as testing needs grow
});
